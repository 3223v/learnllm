[project]
name = "learnllm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mttk", email = "2098517829@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
    "torch==2.11.0",
]

[project.scripts]
learnllm = "learnllm:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
